# نشر منصة استدامة

من جهازك إلى رابط عام، بلا تكلفة. المدة المتوقعة: نحو ٢٥ دقيقة، أكثرها انتظار.

---

## قبل أن تبدأ — تصحيحان

**الأول:** اخترتَ «قاعدة بيانات على الخادم نفسه» مع Render. الاثنان لا
يجتمعان، ولو نفّذتُهما كما هما لفقدتَ بياناتك:

- Render لا تقدّم MongoDB مُدارة — لا في المجاني ولا المدفوع.
- قرص الخدمة المجانية مؤقّت. أي حاوية مونجو بجانب الخادم تفقد كل شركة
  وتقييم وحجز عند كل إعادة نشر، والمجاني يعيد النشر كلما استيقظ من النوم.

**الثاني، وهو في صالحك:** سؤالي افترض أن بياناتك محلية. فحصتُ `server/.env`
فوجدتُها **على Atlas أصلاً**. أي أن الخطوة التي كنتُ سأطلب منك عملها قد
تمّت، ولا شيء لتنقله.

يبقى خيار ثالث لم تختره: خادم VPS مدفوع بـ`docker-compose` ومُجلَّد دائم.
قل إن أردته.

---

## ١ · قاعدة البيانات — جاهزة عندك بالفعل

فحصتُ `server/.env`: قاعدة بياناتك **ليست محلية**. هي على Atlas منذ الآن —
`cluster0.pdadicx.mongodb.net`. فلا شيء لتنشئه هنا؛ الرابط نفسه الذي يعمل
على جهازك هو الذي تضعه في Render.

يبقى تعديلان في لوحة Atlas:

1. **Network Access** ← `Add IP Address` ← `Allow access from anywhere`
   (`0.0.0.0/0`).
   عناوين Render الصادرة ليست ثابتة في الطبقة المجانية، فلا بديل. الحماية
   هنا من كلمة المرور لا من قائمة العناوين.

2. **Database Access** ← إن كانت كلمة مرور المستخدم ضعيفة أو مستعملة في
   مكان آخر، غيّرها الآن. ستُلصق في لوحة Render، وستكون بوابة بياناتك على
   الإنترنت.

خذ الرابط من `server/.env` كما هو (المفتاح `MONGO_URI`).

> إن كانت كلمة المرور تحوي `@` أو `:` أو `/` فرمّزها (`@` تصير `%40`)،
> وإلا انكسر الرابط. رابطك الحالي يعمل، فالغالب أنه مرمّز أصلاً.

---

## ٢ · المستودع

Render تنشر من GitHub، والمشروع ليس مستودعاً بعد.

```bash
cd C:\react\istidamah-platform6666
git init
git add -A
git commit -m "Istedama platform"
```

ثم أنشئ مستودعاً **خاصاً** على GitHub وادفع إليه:

```bash
git remote add origin https://github.com/<username>/istidamah-platform.git
git branch -M main
git push -u origin main
```

`.gitignore` يستبعد `node_modules` وملفات `.env` — تحقّق قبل الدفع أن
`git status` لا يذكر أي `.env`.

---

## ٣ · النشر على Render

1. <https://render.com> ← سجّل بحساب GitHub.
2. **New** ← **Blueprint** ← اختر المستودع.
   يقرأ Render ملف `render.yaml` وينشئ الخدمتين معاً.
3. سيسألك عن قيمتين لخدمة `istidamah-api`:

   | المفتاح | القيمة |
   |---|---|
   | `MONGO_URI` | رابط Atlas من الخطوة ١ |
   | `CLIENT_ORIGIN` | اتركه فارغاً مؤقتاً — الخطوة ٤ |

   وقيمة واحدة لخدمة `istidamah-client`:

   | المفتاح | القيمة |
   |---|---|
   | `VITE_API_URL` | اتركه فارغاً مؤقتاً — الخطوة ٤ |

4. **بعد أول نشر** صار لكل خدمة عنوان. أكمل الحلقة:

   - في `istidamah-client` ← Environment ←
     `VITE_API_URL = https://istidamah-api.onrender.com/api`
     ثم **Manual Deploy** (فايت يدمج القيمة وقت البناء لا وقت التشغيل،
     فإعادة التشغيل وحدها لا تكفي).
   - في `istidamah-api` ← Environment ←
     `CLIENT_ORIGIN = https://istidamah-client.onrender.com`
     ثم أعد التشغيل.

5. تحقّق:

```bash
curl https://istidamah-api.onrender.com/api/health
```

يجب أن يعيد `{"success":true,...,"status":"healthy"}`.

---

## ٤ · البيانات الأولى

من لوحة Render، خدمة `istidamah-api` ← **Shell**:

```bash
npm run seed
```

سيزرع الشركات والمرافق والمناقصات والأخبار، **ولن يزرع حسابات التجربة** —
كلمات مرورها مكتوبة في `seed.js` وهو في المستودع، فزرعها على رابط عام
يعني نشر مدخل مدير مفتوح للجميع.

فسجّل حسابك الأول من صفحة `/register` في الموقع، ثم ارفعه إلى `admin`
من Shell بأمر واحد:

```bash
npm run make-admin -- your@email.com
```

الأمر لا يُنشئ حساباً ولا يضبط كلمة مرور — يغيّر دور من سجّل بنفسه فقط،
فلا يمكن أن يترك خلفه مدخلاً لم تخترْه. وإن أخطأت البريد قال لك ذلك بدل
أن يصمت.

إن كان هذا عرضاً تجريبياً لا أكثر، ولا يهمّك من يدخله: اضبط
`ALLOW_DEMO_ACCOUNTS=true` وأعد تشغيل `npm run seed` فتعود الحسابات
الأربعة. **لا تفعل ذلك على نشر حقيقي.**

---

## ما ينبغي أن تعرفه عن الطبقة المجانية

- **الخادم ينام بعد ١٥ دقيقة خمول.** أول طلب بعدها يستغرق ~٣٠ ثانية بينما
  تستيقظ الحاوية. الواجهة لا تنام — فترى الصفحة فوراً وتتأخر بياناتها.
  إن كان العرض أمام جهة رسمية، افتح الموقع قبلها بدقيقة.
- **Atlas M0 ينام أيضاً** بعد خمول طويل، بتأخير أقصر.
- الترقية إلى Starter (‏٧ دولارات شهرياً) تُلغي النوم وحدها.

---

## قائمة تحقّق قبل أن تعطي الرابط لأحد

- [ ] `JWT_SECRET` مولّد من Render، لا `change_this_super_secret_key`
- [ ] `CLIENT_ORIGIN` عنوان واجهتك، لا `*`
- [ ] `ALLOW_DEMO_ACCOUNTS` غير مضبوط على `true`
- [ ] لا ملف `.env` في المستودع (`git ls-files | grep .env`)
- [ ] مستخدم Atlas له كلمة مرور قوية غير مستعملة في مكان آخر
- [ ] `/api/health` يستجيب، والدخول يعمل، والشركاء والأخبار تظهر

---

## إن انكسر شيء

| العرض | السبب في الغالب |
|---|---|
| الصفحة تفتح والبيانات فارغة | `VITE_API_URL` خاطئ أو بُني قبل ضبطه — أعد النشر يدوياً |
| `CORS` في وحدة تحكم المتصفح | `CLIENT_ORIGIN` لا يطابق عنوان الواجهة حرفياً (بلا `/` في آخره) |
| الخادم يفشل عند الإقلاع | `MONGO_URI` — تحقّق من ترميز كلمة المرور ومن `0.0.0.0/0` |
| `/partners` يعطي 404 عند التحديث | قاعدة إعادة التوجيه في `render.yaml` لم تُطبَّق — أعد النشر |
| أول طلب بطيء جداً | الخدمة كانت نائمة. هذا متوقّع في المجاني |
