# إعادة تصميم الواجهة — ملاحظات وتنفيذ

## ملاحظة على المرفق

الملف المرفق يحتوي على **كود المشروع** لا صور الشاشات. قرأت الكود مباشرة —
سبع عشرة صفحة و‍22 مكوّناً — وبنيت التحليل عليه، وهو أدق من قراءة الصور لأنه
يكشف الأصناف والمسافات الفعلية.

## ما وجدته

المشروع مبني جيداً أصلاً: RTL كامل، خط Tajawal، ثنائي اللغة، حركات framer-motion
منظّمة في `motion/presets`. لذلك لم أعد كتابة المكونات من الصفر — كان الخلل في
**طبقة الهوية** لا في البنية.

| الملاحظة | الأثر على المستخدم | ما فعلته |
|---|---|---|
| أرضية لافندر `#EFF2FC` تحت بطاقات بيضاء | التباين بين البطاقة والخلفية ضعيف، فتضيع حدود الأقسام | أرضية `#F8FAFC` بيضاء-رمادية، فالبطاقة البيضاء صارت السطح الملوّن الوحيد |
| الكحلي `#223E98` بنفسجي الميل | يقرأ كأزرق تقني لا كأزرق مؤسسي | `#0F3260` كحلي ملكي هادئ |
| البرتقالي `#EC9D51` باهت ومستخدم للزينة | نداء الفعل لا يُميَّز عن الزخرفة | `#F97316` محجوز لنداء الفعل والحالة الحيّة فقط |
| ظلال ثقيلة (`0 18px 42px`) على البطاقات الساكنة | الصفحة تبدو مزدحمة بصرياً | ظل خفيف في السكون، والرفع عند التحويم فقط |
| حواف 1.35rem–2.25rem | حواف واسعة تُضخّم البطاقات الصغيرة | 1.25rem للبطاقة، 1.5rem للكتلة الكبرى |
| حقول رمادية `bg-mist` بلا أيقونات | النموذج الطويل يبدو كتلة رمادية واحدة | حقول بيضاء واسعة `py-3.5` بأيقونة داخلية |
| نداءان أساسيان في البطل (`btn-primary` + `btn-quiet`) | المستخدم لا يعرف أيهما الفعل المقصود | نداء برتقالي واحد + ثانوي هادئ |

## التغييرات

**`tailwind.config.js`** — إعادة توجيه الرموز:
`navy-700 #0F3260` أساسي، مقياس كامل من 50 إلى 900 مبني حوله؛
`accent-500 #F97316` مع الاسم القديم `gold` محفوظاً كمرادف حتى لا تنكسر
الأصناف القائمة؛ `canvas #F8FAFC`، `surface #FFFFFF`، `rule #E2E8F0`،
`ink #0F172A`؛ حواف وظلال أخف؛ تدرّج `wash-navy` أعيد بناؤه على الكحلي الجديد.

**`src/index.css`** — الأرضية والتحديد والتمرير وحلقة التركيز على المقياس الجديد؛
`.card` صارت `rounded-2xl shadow-soft`؛ `.card-pad` من `p-6` إلى `p-7 sm:p-8`؛
`.btn-accent` برتقالي بنص أبيض؛ `.field` أوسع وبيضاء؛ أصناف جديدة:
`.field-shell` و`.field-glyph` و`.field-icon` و`.hint` و`.section-gap`.

**`src/components/ui/Field.jsx`** — مكوّن جديد: تسمية مربوطة بـ `htmlFor`،
أيقونة داخل الحقل، تلميح أو خطأ مربوط بـ `aria-describedby`، ويُصيَّر
`input` أو `textarea` أو `select` من الواجهة نفسها. الأيقونة تستخدم `ps-*`
فتنضبط في الاتجاهين بلا قاعدة RTL منفصلة.

**`pages/Facilities.jsx`** — نموذج الحجز التسعة حقول انتقل إلى `Field`:
أيقونة لكل حقل (شخص، هاتف، بريد، مبنى، تقويم، ساعة، مستند)، والمسافة بين
الحقول من `gap-4` إلى `gap-5`، وسعة القاعة صارت تلميحاً تحت حقل عدد الحضور
بدل أن تكون مخفية في `max`.

**`pages/Home.jsx`** — نداء الفعل الأول برتقالي، والمسافة فوقه `mt-10`.

**`pages/Tenders.jsx`** — وسم «قيد الاستقبال» أبيض على برتقالي بدل كحلي على برتقالي.

## التحقق

- `npm run build` ينجح.
- 75 اختباراً في 14 ملفاً تمرّ، منها 5 اختبارات جديدة لمكوّن `Field`
  (ربط التسمية، الإلزام، الخطأ يحلّ محل التلميح، تبديل نوع الحقل، `onChange`).

## ما بقي

الحقول في `Login.jsx` و`Register.jsx` و`CompanyRegister.jsx` و`Advertise.jsx`
ما زالت تستخدم `<input className="field">` مباشرة. تنسيقها تحدّث تلقائياً مع
الرموز الجديدة، لكن الأيقونات الداخلية تحتاج تمريرة ثانية بنفس أسلوب
`Facilities`. الملفات الأربعة كبيرة (`Register.jsx` وحده 2388 سطراً في نسخة
سابقة) فتركتها لجولة تالية بدل تعديل نصفها.
