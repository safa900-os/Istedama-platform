# Vite inlines this into the bundle at BUILD time, so changing it requires a
# rebuild, not a restart. Include the /api suffix.

# Local
VITE_API_URL=http://localhost:5000/api

# Deployed, e.g.
# VITE_API_URL=https://istidamah-api.onrender.com/api
